#spring.main.sources=org.example

logging.config=classpath:logback.xml

# the options from org.apache.camel.spring.boot.CamelConfigurationProperties can be configured here
camel.springboot.name=threescale-camel-plugin-demo

# lets listen on all ports to ensure we can be invoked from the pod IP
server.address=0.0.0.0
management.server.address=0.0.0.0

server.ssl.enabled=false

# lets use a different management port in case you need to listen to HTTP requests on 8080
management.server.port=8081
management.server.ssl.enabled=false

# disable all management endpoints except health
endpoints.enabled = false
endpoints.health.enabled = true

//ssl.key-store=${TRUST_KEY_STORE:${JAVA_HOME}/jre/lib/security/cacerts}

camel.springboot.stream-caching-enabled=true
//camel.component.jetty.keystore=${SSL_KEY_STORE:classpath:keystore.jks}
//camel.component.jetty.ssl-password=password
//camel.component.jetty.ssl-key-password=password

server.tomcat.mbeanregistry.enabled=true